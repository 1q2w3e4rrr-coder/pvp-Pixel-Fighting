AIZEN Step29 Hotfix - U skill no backstep

Problem:
- Original Aizen U / 招1 has no move, movePercent, dash, or moveToTarget calls.
- Current Godot version could still show a fake "backstep then return" because:
  1. ManifestFighter locks skill1/skill2/skill3 world position.
  2. BattlePlaceholder.resolve_original_cross_collision() then pushes the fighter away.
  3. Next frame the position lock pulls the fighter back.

Fix:
- Add is_original_world_position_locked().
- During resolve_original_cross_collision(), if either fighter is in a locked original-world-position action, body separation returns immediately.
- This prevents U / 招1 from being pushed backward by collision separation.
- P2 hit reaction and real hit knockback are not removed; only the fake body-separation push is blocked.

Original basis:
- Aizen 招1 / U only has addQi(10), shine(0xFFFF00), hit windows, endAct(), idle().
- It does not have move / movePercent / dash / moveToTarget, so FighterMain world x should remain stable.

Not changed:
- No effect height change.
- No KO black cleanup change.
- No HUD / map / QiBar / HITS / texture-quality change.
- No attack hitbox change.
