AIZEN Step37 - KO result menu + black rectangle cleanup

User issue:
- After KO, a black rectangle still remains behind the characters / Winner display.
- User requested a battle-end window with:
  再来一局 / 返回选人 / 返回标题.

Diagnosis:
- The black rectangle is likely a legacy overlay layer opened by bisha / winner display.
- Previous cleanup only cleared BishaEffectPlayer by reference.
- If old FightHud.show_winner() creates a black-backed Winner texture or another ColorRect, it can still remain.

Fix:
1. Stop calling old FightHud.show_winner() and show_drawgame() at round end.
2. Add RoundEndMenu:
   再来一局 -> get_tree().reload_current_scene()
   返回选人 -> res://scenes/ui/CharacterSelect.tscn
   返回标题 -> res://scenes/ui/MainMenu.tscn
3. Add force_hide_legacy_blackout_layers():
   - Hides large black ColorRect nodes after KO.
   - Hides CanvasItem nodes whose name contains bisha/black/dark after KO.
   - RoundEndMenu is protected by keep_round_end_ui meta.
4. Continue calling force_clear_bisha_after_round_end().

Changed:
- scripts/battle/BattlePlaceholder.gd

Not changed:
- U skill no-backstep / visual-hitbox sync
- character logic
- hitboxes
- damage
- map
- HUD bars / QiBar / HITS
- texture quality
