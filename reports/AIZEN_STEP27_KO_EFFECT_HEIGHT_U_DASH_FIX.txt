AIZEN Step27 Hotfix - KO black screen / effect height / U skill / dash dust / hit effect quality

Issues fixed:
1. KO after bisha/super could leave the BishaEffectPlayer dark layer visible, causing a black screen.
   Fix: force end_bisha() during KO, timeover, and round-end cleanup.

2. Step26 effect y was still too low.
   Fix: replace the 106.125px correction with a smaller 70px SWF registration correction for common skill/attack effects.
   This moves bsmc, zh3mc, bisha XG, hit/defense/steel/break/catch/KO effects upward by 36.125px.

3. L dash dust should align with the character feet.
   Original FighterEffectCtrler.dash plays XG_rush at fighter.x/fighter.y. Current XG_rush export is an 800x600 canvas whose visible dust bottom is about center+7px.
   Fix: use a dedicated dash origin: character visual foot y - 7px. It does not reuse the 70px general effect correction.

4. U / skill1 should not move the fighter world position.
   Original Aizen 招1 only has addQi(10), shine(0xFFFF00), endAct(), idle(); no move/movePercent/dash/moveToTarget.
   Fix: ground skill1/skill2/skill3 now clear residual velocity and keep a world-position anchor for the duration of the action.

5. Continuous J hit effects looked bad / like they had a white backing.
   Fix: package transparent original common effect PNGs from effects(3).zip into assets/effects/common and use NEAREST texture filtering for common effects.
   No generated image assets were used.

Not changed:
- No HUD logic change.
- No map logic change.
- No QiBar/HITS logic change.
- No attack hitbox/collision frame changes.
